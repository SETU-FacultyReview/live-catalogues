On the module descriptor, the "Cluster" appears as part of the "Module Information". Make this a link to the cluster route. Use the weburl pattern/tutors-module-id pattern for links used module links.

The programme schedule table is to be reordered as follows:

- all mandatory modules appear first (as current)
- elective modules must apper on new rows below the last row of mandatory modules

For programme schedules that have multiple rows of electives, keep modules that are in the same cluster on the same row/rows